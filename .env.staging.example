# Ejemplo de variables para el entorno STAGING
# Rellena con los valores reales en Vercel (Dashboard o CLI)

# MongoDB
MONGO_URI=mongodb+srv://usuario:password@cluster0.mongodb.net/vasoli?retryWrites=true&w=majority

# Orígenes permitidos (separados por comas)
# Añade el dominio del frontend de staging (p. ej. https://tu-frontend-staging.vercel.app)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://tu-frontend-staging.vercel.app

# Token para servicios de Google (si se usan)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=

# Configuración de envío de correos
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Otras variables que uses en producción
NODE_ENV=production
PORT=3000

# Nota: NO subas archivos con credenciales reales al repositorio. Usa el Dashboard de Vercel o `vercel env`.
